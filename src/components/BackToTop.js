import React from 'react';

const BackToTop = () => (
    <div id="backtotop">
        <a href="/#">
            <span style={{display: 'none'}}>^</span>
        </a>
    </div>
);

export default BackToTop;
